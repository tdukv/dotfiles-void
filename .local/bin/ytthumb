#!/bin/sh

url=$1
[ "$url" = "" ] && url="$(xclip -o -selection primary)"

case "$url" in 
    *youtube.com*)
	id=$(echo "$url" | cut -d '=' -f 2)
	feh --class fehfloat "https://img.youtube.com/vi/${id}/hqdefault.jpg"
	;;
    ### add other sites
    *)
	notify-send "Invalid URL"
	exit 1
	;;
esac
